---
// Home.astro
---

<section class="relative min-h-screen bg-gradient-to-br from-[#bddcff] via-white to-[#bddcff] pt-24">
  <!-- Patrón de fondo decorativo -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-10 left-10 w-32 h-32 bg-blue-100 rounded-full opacity-40 blur-3xl animate-float"></div>
    <div class="absolute top-40 right-20 w-40 h-40 bg-blue-200 rounded-full opacity-30 blur-3xl animate-float-delayed"></div>
    <div class="absolute bottom-20 left-1/4 w-48 h-48 bg-blue-100 rounded-full opacity-40 blur-3xl animate-float"></div>
    <div class="absolute bottom-40 right-1/3 w-36 h-36 bg-blue-200 rounded-full opacity-30 blur-3xl animate-float-delayed"></div>
  </div>

  <div class="container mx-auto px-6 py-16 relative z-10">
    <!-- Sección Hero -->
    <div class="text-center mb-16 hero-content">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-6">
        La sonrisa que querés
      </h1>
      <p class="text-lg md:text-xl text-gray-600 max-w-3xl mx-auto mb-2">
        Odontología estética y salud dental con un enfoque profesional y personalizado.
      </p>
      <p class="text-lg md:text-xl text-gray-600 max-w-3xl mx-auto">
        Anímate a sonreír sin límites.
      </p>
    </div>

    <!-- Contenedor principal con imagen y tarjetas -->
    <div class="relative max-w-7xl mx-auto">
      <!-- Grid de 3 columnas -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 items-center">
        
        <!-- Tarjeta izquierda: Servicios Odontológicos -->
        <div class="bg-white rounded-2xl shadow-lg p-8 hover:shadow-xl transition-all duration-300 hover:-translate-y-1 card-left">
          <div class="flex justify-end mb-6">
            <div class="w-12 h-12 bg-blue-500 rounded-lg flex items-center justify-center transition-transform duration-300 hover:scale-110">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
              </svg>
            </div>
          </div>
          
          <h3 class="text-2xl font-bold text-gray-900 mb-3">
            Servicios Odontológicos
          </h3>
          <p class="text-gray-600">
            Conocé todos nuestros tratamientos.
          </p>
        </div>

        <!-- Columna central: Imagen de la doctora -->
        <div class="flex items-end justify-center lg:h-[600px] order-first lg:order-none image-center">
          <div class="w-full max-w-md h-[500px] lg:h-full flex items-end justify-center overflow-hidden">
            <img 
              src="img/details/doctora-frontal.webp" 
              alt="Dra. Marta - Odontóloga" 
              class="w-full h-full object-cover object-top"
            />
          </div>
        </div>

        <!-- Tarjeta derecha: Reseñas De Mis Clientes -->
        <div class="bg-white rounded-2xl shadow-lg p-8 hover:shadow-xl transition-all duration-300 hover:-translate-y-1 card-right">
          <div class="flex justify-end mb-6">
            <div class="w-12 h-12 bg-blue-500 rounded-lg flex items-center justify-center transition-transform duration-300 hover:scale-110">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
              </svg>
            </div>
          </div>
          
          <h3 class="text-2xl font-bold text-gray-900 mb-3">
            Reseñas De Mis Clientes
          </h3>
          <p class="text-gray-600">
            La opinión de mis pacientes.
          </p>
        </div>

      </div>
    </div>
  </div>
</section>

<style>
  /* Animaciones de flotación para elementos de fondo */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float 6s ease-in-out infinite;
    animation-delay: 3s;
  }

  /* Animación de entrada para el hero */
  .hero-content {
    opacity: 0;
    animation: fadeInUp 1s ease-out forwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Animación de entrada para tarjeta izquierda */
  .card-left {
    opacity: 0;
    animation: slideInLeft 0.8s ease-out 0.3s forwards;
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* Animación de entrada para imagen central */
  .image-center {
    opacity: 0;
    animation: scaleIn 0.8s ease-out 0.5s forwards;
  }

  @keyframes scaleIn {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Animación de entrada para tarjeta derecha */
  .card-right {
    opacity: 0;
    animation: slideInRight 0.8s ease-out 0.3s forwards;
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
</style>